--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu

# Note: As per Backend_Implementation_Guide.md, versions are not frozen here 
# to allow validation against the target Python/CUDA environment first.

# --- Core Web API & Validation ---
fastapi
uvicorn[standard]
python-multipart
aiofiles
pydantic
pydantic-settings
orjson

# --- Database & Migrations ---
sqlalchemy
asyncmy
alembic
# Optional helper for mysql sync operations / cryptography authentication support
cryptography

# --- PDF Processing & OCR ---
# CPU-only deployment. For GPU support, replace paddlepaddle with paddlepaddle-gpu
# and install torch with CUDA wheels. See PaddleOCR/EasyOCR docs for details.
pymupdf
paddlepaddle==3.3.1
paddleocr==3.7.0
easyocr
pillow
numpy

# --- Machine Learning, Embeddings & LLM ---
# Note: For CUDA-enabled environments, ensure PyTorch is installed with appropriate CUDA wheels
torch
transformers
accelerate
safetensors
sentence-transformers
gpt4all
ctransformers
llama-cpp-python

# --- Vector Database ---
qdrant-client

# --- Utilities & Clients ---
httpx

# --- Testing Suite ---
pytest
pytest-asyncio

# --- Authentication & Security ---
python-jose[cryptography]
passlib[bcrypt]
email-validator
